import React from 'react'

const Headline = ({title}) => {
    return (
        <div className="headline-wrapper">
            <h2>{title}</h2>
        </div>
    )
}

export default Headline
